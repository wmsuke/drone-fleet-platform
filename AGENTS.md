# 開発ルール

## このプロジェクトについて

ドローンを題材に、複数デバイスの状態監視と遠隔コマンドを扱う管理基盤を作る。

最初はローカルで仮想ドローン10台を動かす。AWS IoT、通信断対応、OTA、ROS 2連携は段階的に追加する。AWS対応後もローカルだけで動く構成を維持する。

## 作業の進め方

- 作業前に対象Issue、関連するコード、設計文書を読む。
- Issueの完了条件を満たす範囲で変更する。無関係なリファクタリングは含めない。
- 将来の機能を先回りして実装しない。共通化や依存ライブラリの追加には具体的な理由を持つ。
- 小さな実装上の判断は既存コードに合わせて進める。通信仕様やサービス間の責務を変える場合は、理由と影響を説明する。
- 実装に合わせて関連文書を更新する。未実装の機能を動作済みとして記載しない。

## 設計

- 採用技術とサービスの責務は `docs/architecture.md` を参照する。
- MQTTトピックとメッセージ形式は `docs/protocol.md` を参照する。
- 実装するPhaseと対象範囲は `docs/roadmap.md` を参照する。
- 通信仕様の型と実行時検証は `packages/protocol` に集約する。
- MQTT受信処理とHTTP APIの責務を分ける。
- 接続状態と飛行状態を区別する。
- コマンドの受領確認と実行完了を区別する。

## 検証

- 変更に応じて、型チェック、lint、テスト、ビルドを実行する。
- 不具合修正では、必要に応じて再発を検出できるテストを追加する。
- 実装の内部構造をなぞるだけのテストは避け、外から確認できる振る舞いを検証する。
- 実行できない検証があれば、理由と未確認の範囲を報告する。
- 開発コマンドはリポジトリに定義されたものを使う。まだ存在しないコマンドを実行済みとして扱わない。

## 秘密情報

- 認証情報、秘密鍵、実環境の設定ファイルをコミットしない。
- `.env.example`にはダミー値と設定項目の説明を記載する。
- ログやテストデータにも認証情報を含めない。

## 文章

- Issue、README、設計書、ADR、PRのタイトルと説明は日本語で書く。
- 技術名、ファイル名、API、コードの識別子は元の表記を使う。
- 説明は簡潔な常体を基本にする。目的、変更内容、理由を具体的に書く。
- 大げさな表現、定型的な前置き、同じ結論の繰り返しを避ける。
- 見出し、箇条書き、太字は読みやすくなる場合に使う。小さな変更に大きなテンプレートを当てはめない。
- PRには変更内容と検証結果を書く。制約や未確認事項があれば併記する。
